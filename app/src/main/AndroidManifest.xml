<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BOOKLIBRARY">
        <activity android:name="com.clubdevfire.booklibrary.LibraryActivity" />
        <activity
            android:name="com.clubdevfire.booklibrary.DocumentsActivity"
            android:label="@string/title_activity_documents"
            android:theme="@style/Theme.BOOKLIBRARY.NoActionBar" />
        <activity android:name="com.clubdevfire.booklibrary.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>